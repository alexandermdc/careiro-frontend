name: CI/CD Frontend

on:
  push:
    branches:
      - main
      - develop
      - 'feat/**'
  pull_request:
    branches:
      - main
      - develop

jobs:
  # Job 1: Build e Test
  build-and-test:
    name: Build e Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      # 1. Checkout do código
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      # 2. Setup Node.js
      - name: 🔧 Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      # 3. Instalar dependências
      - name: 📦 Instalar dependências
        run: npm ci --legacy-peer-deps

      # 4. Lint (verificar código)
      - name: 🔍 Lint
        run: npm run lint || echo "Lint warnings encontrados"
        continue-on-error: true

      # 5. Build da aplicação
      - name: 🏗️ Build
        run: npm run build

      # 6. Upload dos artifacts
      - name: 📤 Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist-${{ github.sha }}
          path: dist/
          retention-days: 7

  # Job 2: Build Docker Image
  docker-build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')

    steps:
      # 1. Checkout do código
      - name: 📥 Checkout código
        uses: actions/checkout@v4

      # 2. Setup Docker Buildx
      - name: 🐳 Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 3. Login no Docker Hub (apenas se tiver secrets configurados)
      - name: 🔐 Login Docker Hub
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        continue-on-error: true
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 4. Extract metadata
      - name: 📋 Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ secrets.DOCKER_USERNAME || 'local' }}/agriconnect-frontend
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      # 5. Build Docker Image
      - name: 🏗️ Build Docker Image
        id: docker_build
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.ref == 'refs/heads/main' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # 6. Image digest
      - name: 📝 Image digest
        if: steps.docker_build.outputs.digest != ''
        run: echo ${{ steps.docker_build.outputs.digest }}

  # Job 3: Deploy (opcional - para quando tiver servidor)
  # deploy:
  #   name: Deploy to Server
  #   runs-on: ubuntu-latest
  #   needs: docker-build
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #     - name: 🚀 Deploy via SSH
  #       uses: appleboy/ssh-action@master
  #       with:
  #         host: ${{ secrets.SERVER_HOST }}
  #         username: ${{ secrets.SERVER_USERNAME }}
  #         key: ${{ secrets.SERVER_SSH_KEY }}
  #         script: |
  #           cd /path/to/app
  #           docker-compose pull
  #           docker-compose up -d
  #           docker system prune -f
